# Customization: Replace [SQLServerInstanceName] with your SQL Server instance name.
# Use Case: Displays details of individual job steps.

Invoke-Sqlcmd -ServerInstance "[SQLServerInstanceName]" -Query "SELECT * FROM msdb.dbo.sysjobsteps"
