# Customization: Replace [SQLServerInstanceName] with your SQL Server instance name.
# Use Case: Lists linked servers configured in SQL Server.

Invoke-Sqlcmd -ServerInstance "[SQLServerInstanceName]" -Query "SELECT * FROM sys.servers"
