# Customization: Replace [SQLServerInstanceName] with your SQL Server instance name.
# Use Case: Displays SQL Server configuration options and their current values.

Invoke-Sqlcmd -ServerInstance "[SQLServerInstanceName]" -Query "EXEC sp_configure"
