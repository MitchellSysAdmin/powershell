# Customization: Replace [SQLServerInstanceName] with your SQL Server instance name.
# Use Case: Displays SQL Server logins and their associated roles.

Invoke-Sqlcmd -ServerInstance "[SQLServerInstanceName]" -Query "SELECT * FROM sys.server_principals"
